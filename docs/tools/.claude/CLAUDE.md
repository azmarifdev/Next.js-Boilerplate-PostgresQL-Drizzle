# Claude Project Guidance

- Use REST endpoints under `/api/v1`
- Keep PostgreSQL + Drizzle only
- Keep Better Auth as default auth mode
- Keep React Query as default state mode
- Treat Redux as optional and non-default
- Keep optional modules inside `src/modules/optional`
- Preserve workflow behavior unless explicitly requested
